import { expect } from 'chai'
import { DynamicTask } from './dynamic-task'

describe('basic tests', () => {
  describe('metadata', () => {
    it ('workflow attached', async () => {
      // const task = new DynamicTask(get(3))
      // const cls = makeTask(get)
      // const obj = new cls()
      // expect(obj.unitverse.name).to.equal('get')
      // expect(obj.run(3)).to.equal(3)
    })
  })
})
