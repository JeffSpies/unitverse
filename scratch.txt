// (n:number) => n+1,
      // (n) => [ {'count': n}, {'count': 30}],
      // ({ engine }) => { return (i) => {console.log(engine); return i }},
      // chain()
      //   .map('count')
      //   .value(),
      // _.sum,
      // log(input => `Result: ${input}`),
      // log('done!')